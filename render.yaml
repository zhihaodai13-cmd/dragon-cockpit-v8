services:
  - type: web
    name: stock-analysis
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerCommand: uvicorn api.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: WEBUI_HOST
        value: "0.0.0.0"
